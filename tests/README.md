# Tests

From the `tests/` directory, run all tests (TAP):

```bash
./run.sh
```

Include captured CLI output:

```bash
./run.sh -v
```

Run a single test from the same directory, for example:

```bash
./smoke.sh
```

See the other `*.sh` files in this directory for the full list.
